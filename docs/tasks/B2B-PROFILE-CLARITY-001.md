# B2B-PROFILE-CLARITY-001 — PC-B

The owner clarified that the branch dropdown must show the counterparty agency's branches. The dossier profile now reads that agency's existing Master Organization addresses, with label/city/primary/inactive indicators, selected-address details and the existing create/edit/delete dialogs. After a create, the new address is selected; after deletion, selection falls back to an existing primary/active address. No fictional branches or external address IDs are submitted as IAM scope. The internal responsible branch remains separate (plain text when there is only one authorized branch).

Account manager is described as the internal employee responsible for following up with the agency, with the existing active same-branch staff selector. The cooperation-status card links directly to Contracts → Framework agreement. Status is still read from the real operational profile; existing independent approval activates an UNDER_REVIEW profile, and selfapproval/access rules remain unchanged.

Company national ID replaces the hardcoded unavailable placeholder. Master Data owns an optional unique `nationalId` column, generic values/attributes API, Persian/Arabic digit normalization, 11-digit/LEGAL validation and a friendly duplicate conflict. Existing omitted values are preserved and explicit blank clears them. Registration and editing forms accept it, and the dossier/directory display it. It is entered from company records; no registry integration or automatic verification is claimed. Natural-person IDs are not stored here. See DECISIONS and DATA_MODEL.

## Validation and rollout

- 90 Organizations Web tests, 479 Master Data/B2B API tests and 14 PostgreSQL B2B transaction tests pass. API/Web/Database lint and typecheck pass; Prisma validation/generation and API/Database builds pass. The first broad whole-project test run was stopped under heavy resource contention after unrelated timeouts; focused tests were rerun with one worker. No unrelated code was changed.
- Actual React browser fixture checks: external branch switching updates address details while the internal branch remains unchanged; edit saves and updates the selected option; national ID renders; review button selects the actual framework section; console error log is empty. Production authenticated browser checks depend on the existing login session.
- Migration `20260909193000_master_organization_national_id` was rehearsed after restoring a fresh backup into disposable PostgreSQL 18. All existing data across 127 business tables was preserved; unique, format and LEGAL-only constraints and explicit clearing were exercised. The operational database has 46 known finished historical migrations; this is the only pending migration. Preserve their stored checksums and apply only this additive migration, with a fresh cutover backup. No historical migration repair, reset, IAM grant, real national ID seeding or dependency change.
- Branch `codex/pc-b-b2b-profile-clarity` is based on 65042b4 and retains the current combined runtime plus PR132–136/138 dependencies. Review targets develop; no merge is part of this change. Runtime activation and the final Web build are recorded after cutover.

## Completed cutover

- Source dbd7329 / Web build hr005-b0658359a760157c is active on port3100 (PID12100); the rebuilt API is healthy on4190 (PID19300). Database `rubi_hr_current_20260908` and existing Documents storage are preserved. Organizations/login/HR/Documents return HTTP200 and the runtime endpoint matches the source. The production browser goes to login; authenticated production writes were not exercised.
- All 40 Web routes build successfully. All four CI gates in run34450246803 pass, including the full monorepo test suite, production build, quality gate and PostgreSQL migration/seed gate.
- Local migration checksum f050970472fa0fe21c04e17f2f6523f7e900b13ad854b9b18371da092bb46079 was applied alone after a fresh cutover backup. Private backup/evidence is under `C:/Users/admin/Rubi-backups/b2b-profile-identity`; all 127 business-table digests and 46 historical migration records/checksums were preserved. Legacy company IDs remain NULL; no synthetic identity was inserted into the operational database.
- Final real-form fixture verification additionally confirms that organization editing preloads national ID, accepts a change and displays it in the dossier. A missing version in the isolated test fixture was corrected before that check; application source required no further fix. Fixtures are separate from the operational API and were stopped after verification.
- Draft PR140: https://github.com/nirvanamahlou/Rubi/pull/140. Implementation/migration reservation released. Documentation-only follow-up does not change the built source. Other computers must integrate retained dependencies, apply the additive migration and regenerate/build Prisma/API/Web before running this version.
