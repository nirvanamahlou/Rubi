# DOCUMENTS-005-ARCHIVE-ACTIONS-FIX

- Computer: `PC-B`
- Branch: `codex/pc-b-documents-archive-actions-fix`
- Base: `origin/develop@85204a4`
- Status: `READY_FOR_REVIEW`

## هدف

چهار ابزار باقی‌مانده مدیریت آرشیو باید بدون خروج از بخش مدیریت، فهرست واقعی و فیلترشده خود را نمایش دهند. فرم‌های عملیات رکورد نیز باید پس از تکمیل ورودی‌ها با Submit استاندارد اجرا شوند و در صورت ورودی ناقص یا خطای سرویس، علت را به‌صورت روشن نشان دهند.

## محدوده

- `apps/web/src/modules/documents/**`
- تست‌های Web ماژول Documents
- همین سند و ورودی محدود Work Assignment

## خارج از محدوده

- Prisma Schema، Migration و Seed
- قرارداد عمومی Documents و API
- Dependency و Lockfile
- Shared Calendar و فایل‌های UI مرکزی
- `docs/PROJECT_STATUS.md` و قفل‌های `PC-A/SALES-CONTRACTS-001`

## معیار پذیرش

1. کلیک هر کارت مدیریت آرشیو همان منو را فعال نگه دارد و عنوان ابزار انتخاب‌شده را نمایش دهد.
2. فهرست اختصاصی ابزار با Query واقعی Backend بارگذاری شود و دکمه بازگشت به ابزارهای مدیریت داشته باشد.
3. پاک‌سازی فیلترها، فیلتر پایه همان ابزار را از بین نبرد.
4. فرم عملیات گروهی/تک‌رکوردی و حذف دائمی با Submit فرم اجرا و خطای اعتبارسنجی فارسی نمایش داده شود.
5. Browser QA روی داده آزمایشی، ویرایش، آرشیو و بازیابی را بدون باقی‌گذاشتن تغییر موقت تأیید کند.

## نتیجه

- کارت‌های مدیریت آرشیو دیگر کاربر را به «همه اسناد» منتقل نمی‌کنند و منوی مدیریت آرشیو فعال می‌ماند.
- هر ابزار عنوان، توضیح، فیلتر پایه، فهرست واقعی و بازگشت مستقل به ابزارهای مدیریت دارد.
- «اسناد تحت مسئولیت من» از فیلتر امن سمت سرور برای کاربر جاری استفاده می‌کند.
- عملیات گروهی/تک‌رکوردی و حذف دائمی با Submit استاندارد اجرا می‌شوند و ورودی ناقص پیام فارسی روشن دارد.
- فرم ویرایش مقادیر خالی فیلدهای اختیاری را نیز صریح ارسال می‌کند.

## اعتبارسنجی

- Web tests: `603/603`
- Web lint: موفق
- Web typecheck: موفق
- Web Production Build: موفق، ۳۴ Route
- Browser QA: هر چهار ابزار داخل مدیریت آرشیو، فهرست ۱۵ سند تحت مسئولیت، خطای دلیل کمتر از ۵ نویسه، Dropdown ویرایش، آرشیو و بازیابی واقعی از ابزار اختصاصی
- داده آزمایشی حذف نشد و همه تغییرات موقت QA بازگردانده شد.
- نسخه نهایی روی `http://localhost:3100/documents` با API فعال `http://localhost:4000/api/v1` در حال اجرا است.
