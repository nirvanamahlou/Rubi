# HR-001 — PC-B — Phase A / READY_FOR_REVIEW

- Date: 2026-09-06. Branch: `codex/pc-b-hr-foundation`.
- Draft Pull Request: [#99](https://github.com/nirvanamahlou/Rubi/pull/99) targeting `develop`; no merge or force push.
- Base: `origin/develop@092109d`. Independent worktree: `C:/Users/admin/Rubi-hr-foundation`.
- Reserved exclusively: `apps/api/src/hr/**`, `apps/web/src/modules/hr/**`, `apps/web/src/app/(crm)/hr/**`, the HR-owned `human-resources/page.tsx` entry, and this task document.
- Central Docs and Migration remain PC-A/SALES-CONTRACTS-001. Per explicit user instruction this document substitutes for central assignment/status edits. Dependency/Lockfile is RELEASED and not acquired. Shared contracts/root exports are not acquired or edited.
- Persistence: **BLOCKED_FOR_MIGRATION_HANDOFF** (`BLOCKED_FOR_MIGRATION`). No controller, repository, Prisma changes, migration or business seed.
- Public HR contracts: local proposal only, **BLOCKED_FOR_PUBLIC_CONTRACT** until IAM and producer/consumer handoff.
- PR #90 is OPEN; #94 is MERGED at baseline. No files from their changesets are edited.
- Existing employee permission names in product docs are illustrative; the requested `hr.read`/`hr.sensitive.read` vocabulary is a module-local proposal, not a change to IAM grants. Phase B must explicitly reconcile names with IAM. No aliases implicitly grant access.
- DEC-OPEN-006 and DEC-OPEN-013 remain unresolved. No retention duration, statutory payroll rule or leave entitlement is invented.
- UI assumptions: internal authenticated desktop/corporate network, responsive mobile; WCAG AA target, implementation owner PC-B (formal acceptance owner pending). Targets, not measured claims: p75 LCP <=2500ms, INP <=200ms, CLS <=0.1; added route JS <=80KB gzip; Lighthouse accessibility >=95, performance >=90.
- Follow-up 2026-09-06: the owner requested a dedicated new-employee form based on the employee-list fields, with separate Persian first and last name fields replacing the generic display-title field. This remains inside the existing HR Web reservation and is session-only until the Phase B persistence/public-contract gates are released.
- Follow-up 2026-09-06: removed the bulk employee entry action from the Employees header per product-owner request.
- Follow-up 2026-09-06: made the preview organizational chart editable with structure-aware create/edit forms for unit identity, branch, hierarchy, manager, position capacity, effective date and status. Changes remain session-only until Phase B persistence contracts are released.
- Follow-up 2026-09-06: added tab-specific session-only create/edit forms and populated lists for branches, units, jobs/positions, grades and employee groups. Cross-field selectors derive from the preview catalogs and dependent display references follow renamed branches, units and grades.
- Follow-up 2026-09-06: removed employee referrals from both the custom recruitment tabs and Frappe-style workspace. Replaced the remaining generic create/edit preview form with section-aware forms derived from each subpage purpose and data columns; field controls infer dates, amounts, workflow states, employees, managers, branches, units, currencies and access levels.
- Follow-up 2026-09-06: replaced shared placeholder records with a dedicated preview dataset for every HR subpage. Recruitment, lifecycle, contracts, time, development, expenses, benefits, assets, fleet, requests, finance, reports, payroll, settings and every employee-profile tab now expose different domain columns, populated example values and status; contextual forms consume those same columns.
- Follow-up 2026-09-06: aligned the employee-list filters in one responsive grid. Search, status, branch and unit now use the same visible-label structure and exact 40px control height; the filter button shares their baseline and the mobile layout collapses to one column.

## Delivery scope and status

| Capability | Status | Evidence / limit |
| --- | --- | --- |
| Independent HR domain and typed resource ports | IMPLEMENTED | Employee/org, recruitment, lifecycle, attendance/check-in, shift/leave, performance/training, expenses, payroll/tax/benefits, fleet, workflow/customization/integration resources |
| Domain rules and command preparation | IMPLEMENTED | UTC, Decimal string, ranges, staffing budget/headcount, interview panels/scores, GPS bounds, attendance calculation version, multi-currency rate, assignment overlap/cycles/capacity, contract expiry/activation, leave maker/checker, payroll population, odometer chronology, optimistic version and idempotency validation |
| Deny-by-default access and masking | IMPLEMENTED | Pure policies; trusted IAM adapter and durable audit adapter are not wired |
| Persian RTL workspace matching the supplied HR reference | PREVIEW_ONLY | Unified 18-card hub with embedded entry points to 9 Frappe-style workspaces, 19 section/detail screens, full capability tab sets, explicit `preview-*` records |
| Create/View/Edit, search and filters | PREVIEW_ONLY | Dedicated new-employee form validates every list field and adds a session-only row; other dialog/search interactions remain ephemeral, with no browser persistence or backend writes; sensitive controls disabled |
| KPI, payroll and reports | PREVIEW_ONLY | Dashboard and workspace KPIs are explicitly synthetic and populated; payroll execution and PDF/XLSX exports remain disabled pending approved public contracts |
| Database, durable audit, idempotency receipts, FK constraints | BLOCKED_FOR_MIGRATION | Exact persistence status: `BLOCKED_FOR_MIGRATION_HANDOFF` |
| IAM, Documents, Master Data, Legal Entity, Finance integrations | BLOCKED_FOR_PUBLIC_CONTRACT | Consumer ports only; no cross-module internal imports or queries |

The existing `/human-resources` entry now redirects to the canonical `/hr` workspace.
The central navigation/breadcrumb file is not edited: `/hr` currently gets the shell's
unknown-route breadcrumb and no active sidebar highlight. A central UI handoff must map
`/hr` to Human Resources. The workspace itself has its own title and active section links.
No operational HR access is granted by the UI preview; the real module remains unwired.

## Frappe HR capability comparison

The comparison baseline is the official
[Frappe HR repository](https://github.com/frappe/hrms) and
[documentation index](https://docs.frappe.io/hr/introduction), reviewed on 2026-09-06.
"Implemented" below means the Phase A domain contract and reviewable UI surface exist. It
does not mean persistence, legal calculation or external integration is operational.

| Capability family | Rubi Phase A coverage | Operational gate |
| --- | --- | --- |
| Employee file, branch/unit/position/grade/group and org chart | IMPLEMENTED | FK persistence and trusted Master Data/IAM adapters |
| Staffing plan, requisition, opening, applicant, multi-round interview, feedback and offer | IMPLEMENTED | Documents/IAM adapters, workflow persistence and candidate conversion transaction |
| Onboarding, promotion, transfer, skill map, separation, exit interview and final settlement intent | IMPLEMENTED | IAM provisioning and Finance settlement contracts |
| Check-in/out, GPS, biometric/import source, calculated hours, lateness, early departure and correction request | IMPLEMENTED | Device/mobile ingestion, approved calculation rules and durable audit |
| Shift definition, assignment/request/swap, recurrence and roster | IMPLEMENTED | Persistent scheduler and approved shift/overtime policy |
| Leave type/policy/allocation/ledger, holiday list, compensatory leave and encashment flag | IMPLEMENTED | Approved entitlement rules and Finance handoff for encashment |
| Performance cycle, KRA/goal, weighted review, self-review and feedback | IMPLEMENTED | Workflow persistence and scoped reporting |
| Training program, event, participant outcome, feedback and certificate reference | IMPLEMENTED | Documents/public provider contracts |
| Travel, advance and expense claim with multi-currency settlement | IMPLEMENTED | Approved rate source and Finance reconciliation contract |
| Salary structures/components, run/slip, extra payment, bonus, incentive, arrear and correction | IMPLEMENTED | Statutory formula rules, migration, maker/checker and Finance posting |
| Tax slabs/declarations/proofs, benefits, loans and gratuity | IMPLEMENTED | Jurisdiction-specific approved legal rules |
| Vehicle registry and usage log | IMPLEMENTED | Assets/Finance ownership contracts |
| Employee/manager self-service, mobile surface, workflows, roles, notifications and reminders | IMPLEMENTED | IAM, notification service and operational endpoints |
| Custom field/form/print/report definitions, API/Webhook and multi-company issuer scope | IMPLEMENTED | Approved schema governance, public contracts and secret reference adapter |
| Persian calendar | IMPLEMENTED_IN_UI | Server/date-storage acceptance remains UTC |
| Iran insurance and tax exports | BLOCKED_FOR_APPROVED_RULES | No statutory layout or calculation is invented |
| Iran bank export | BLOCKED_FOR_PUBLIC_CONTRACT | Requires an approved bank/payment contract |

## Permission matrix (proposal, not IAM grants)

All actions require the exact permission AND assigned branch. A branch other than the
active branch additionally requires `hr.scope.multibranch`; this never grants an
unassigned branch. Legal Entity context does not alter the employee scope.

| Scope | Allowed ceiling, still requiring explicit grants |
| --- | --- |
| SELF | Own `hr.read`, `hr.leave.request`, `hr.documents.read` only |
| MANAGER | Explicit managed employee IDs only: read, contract.read, attendance.read, leave.approve, performance.read/manage, documents.read; never self approval |
| HR | Explicitly granted requested HR permissions within assigned branch scope |
| Missing actor/grant/scope | Denied |

The full 28 functional permissions plus `hr.scope.multibranch` are declared in
`apps/api/src/hr/hr.policy.ts`. Sensitive reveal additionally requires a valid purpose,
10–500 character reason and successful audit before a field read. Only the purpose code,
field, actor, subject, UTC and trace ID enter audit; free text/value do not. Read projections
exclude private monetary, assessment and document fields. Documents access requires matching
stable ID, authorization and CLEAN status; the public adapter must recheck/audit on open.
Legal hold always prevents disposal; no approved retention policy also denies disposal.
Contract activation and review finalization require a distinct maker/checker. Leave decisions
also reject a second account mapped to the requesting employee.

## Verification

- `pnpm install --frozen-lockfile`: PASS; no dependency/manifest/lockfile change.
- Full monorepo lint: PASS. API lint repeated after final command additions.
- Web and API typecheck: PASS after fixing CommonJS test path handling. Web typecheck
  repeated after adding workspace and deep-link query parameters.
- Production monorepo build: PASS, including dynamic `/hr` and 35 generated pages.
  API build repeated after adding command preparation.
- Targeted HR foundation/boundary tests: 51 API PASS. The expanded Web HR UI suite has
  46 focused tests and all 46 PASS, including all nine workspace definitions, populated
  metrics, valid section/tab targets and deep-link tab selection.
- Full API suite: 845 PASS, 70 opt-in PostgreSQL SKIP.
- Full Web suite: 660 PASS, 1 FAIL in unchanged Customers `customer-workspace.spec.ts:199`.
  The test expects an LF literal across lines; this Windows checkout has CRLF. Both test
  and source match baseline `092109d` (verified with `git diff --exit-code`). This task does
  not normalize or edit protected Customers files. The full monorepo test command therefore
  returns failure; it is not reported green.
- Prisma generation only used a synthetic loopback URL on unused port 59999. No database
  connection, migration, seed or persistence acceptance test was run.
- Full monorepo lint, typecheck and production build: PASS on 2026-09-06.
- Browser QA: production server on isolated loopback port 3188, fresh headless Edge context
  and synthetic UI-only cookie. Tested all 19 section/detail URLs, the 18-card hub,
  search/empty, 15 employee tabs, shared Persian calendar, ephemeral dialog and success
  notice, payroll preview, Iran-localization gates, mobile self-service tab, and the
  `/human-resources` redirect. Desktop 1440×1000 and mobile
  390×844: no horizontal overflow, failed HTTP response, console error or page error.
  This is UI verification, not an IAM/login integration test. No real account or data used.
- Visual screenshots inspected locally. Lighthouse and field Core Web Vitals were not measured;
  targets above are not a claimed performance or accessibility certification.
- Dashboard overview revision: all eight KPI cards use explicit synthetic values, the date,
  period, branch and unit controls are interactive, and the filtered values, trend and employee
  composition update together. The dashboard-level employee-entry and export actions were
  removed. Browser verification covered the default 86-person snapshot and a 10-person filtered
  snapshot without exposing or claiming operational HR data.
- Local production build verification: when `NEXT_PUBLIC_API_BASE_URL` is absent, browser calls
  discover port 4000 only for loopback hostnames. Login, Legal Entity context and `/hr` loaded on
  `localhost:3100` without the API-configuration error; non-local hosts remain fail-closed.
- Frappe workspace revision: one unified 18-card HR hub embeds contextual entry points to
  Expenses, HR Setup, Leaves, Payroll, Performance, Recruitment, Shift & Attendance,
  Tax & Benefits and Tenure; the separate nine-card launcher was removed. Every workspace has
  three populated synthetic metrics, a populated eight-month trend and the requested Masters &
  Reports groupings. All 164 directory entries resolve to an existing Rubi HR section and, where
  applicable, a validated tab. Browser QA confirmed exactly 18 capability cards and nine unique
  embedded workspace links, covered all nine workspace URLs, confirmed 11–30 links per workspace,
  absence of `No Data` and API setup errors, one clicked deep link, and the 390×844 responsive
  layout without document overflow.
- New-employee form follow-up: the employee action now opens a dedicated grouped form for
  first name, last name, personnel code, employment type, branch, unit, position, direct
  manager, start date and status. Required and duplicate-code validation is inline and
  accessible. A valid submission derives the table name from first and last name and adds a
  clearly session-only row. Web lint, typecheck, the 46 HR tests and production build all pass;
  browser QA covered empty validation, Persian calendar selection, successful insertion and
  the final visible primary action on `localhost:3100`.
- Contextual-form follow-up: employee referrals were removed from recruitment navigation and
  the Frappe recruitment directory. Generic create/edit dialogs now derive their labels,
  controls, required state and domain options from each active subpage's purpose and visible
  data columns; employee and organization forms retain their richer specialized schemas.
- Breadcrumb follow-up: the shared application shell now reads validated HR `section` and
  `workspace` query parameters and renders `فضای کاری CRM / منابع انسانی / <بخش فعال>` on
  every HR detail or Frappe workspace page. Unknown values fall back to the HR root. Web lint,
  typecheck and 57 targeted navigation/HR tests pass. This PC-B follow-up only extends the
  shared breadcrumb files already assigned to PC-B; no other central navigation behavior changed.
- Date-filter cleanup: the default `شهریور ۱۴۰۵ · داده نمایشی` caption was removed from
  HR date filters while retaining their grid alignment and the dashboard's explicit guidance.
- Breadcrumb cleanup: the duplicate page-local `خانه ‹ منابع انسانی ‹ ...` trail was removed
  from every HR heading; the shared application breadcrumb remains the single navigation trail.
- Branch naming follow-up: every HR branch selector, form, organization catalog, chart and
  preview dataset now uses the two approved names `نیایش سیر` and `جهان باستان`; aggregate
  report filters may still expose `همه شعبه‌ها` without introducing a third branch.
- Calendar visibility follow-up: the shared date picker now renders in a viewport portal,
  flips above fields near the bottom edge, clamps horizontally and vertically, and follows
  scroll/resize changes so calendars opened inside HR dialogs remain visible.
- Organization-chart edge follow-up: every valid parent-child relationship now renders as a
  responsive SVG connector behind the nodes. Edge geometry is recalculated after node edits,
  additions and viewport resizing while preserving the existing editable tree structure.
- Session record operations follow-up: every HR table that exposes an `عملیات` column now
  provides working edit and delete controls. Generic forms are prefilled from the selected row
  and replace that row on save; employee and organization records retain their specialized
  forms. Confirmed deletes remove the record from the active in-memory dataset immediately.
  Organization-chart deletion also removes descendants to prevent orphan nodes. These preview
  mutations remain session-only and do not claim backend persistence or operational hard-delete
  semantics.
- Automatic form-code follow-up: create forms now assign their record code without user input.
  Employee codes increment as `HR-1001`, organization-chart nodes as `HR-ORG-001`, and branch,
  unit, position, grade and employee-group catalogs use their own sequential prefixes. Generic
  HR forms generate a scoped section/tab identifier. Code fields are read-only, while edit forms
  retain the existing record code.
- Organization-chart catalog synchronization follow-up: every session create, edit or delete in
  branches, units and positions now updates the chart immediately. Branches become management
  roots, units follow their selected branch and parent hierarchy, connector edges follow those
  relationships and position capacity is recalculated from the position catalog.
- Required-field indicator follow-up: every required employee, organization, catalog and
  contextual HR field now renders the same visible red asterisk from its validation metadata and
  carries the corresponding form-required semantics. Conditional fields only show the indicator
  while the selected structure makes them mandatory.
- Employee-type form follow-up: the former employee-group entry is presented as `نوع کارکنان`.
  Its code remains automatic and read-only, while the only value entered by the user is the
  required employee-type name; group-type, description, date and status inputs were removed.
- Recruitment row visibility follow-up: generic HR dataset overrides, including every recruitment
  tab, are now restored from validated session storage and written back after each create, edit or
  delete. The `/hr` page no longer forces a root workspace remount for query navigation, so a newly
  submitted recruitment row remains visible after closing the form and returning to the section.
- Automatic history follow-up: employee dossier history, vehicle-use history and HR Audit are now
  read-only append-only views without create, edit or delete forms. Employee mutations append to
  dossier history and Audit, vehicle registry mutations append to vehicle history and Audit, and
  generic HR plus organization mutations append to Audit automatically within the preview session.
- HR hub copy follow-up: the redundant `امکانات Frappe HR` label was removed from the related
  workspace links beneath each capability card while preserving all nine workspace entry points.
- HR interaction and export follow-up (2026-09-07): removed the dashboard month/year hint, the
  internal module-boundary copy and the remaining Frappe capability eyebrow. Every section-level
  create/export action now shares the date-range action row. The employee list applies its visible
  search/status/branch/unit filters to a populated XLSX export and links every row to a selected
  employee 360 dossier. Employee dossier tabs support session create/edit/delete where appropriate,
  and the selected employee accepts a validated session profile photo. The employee-type catalog was
  removed, organization levels were expanded, and organization manager/unit selectors now derive from
  current employees and the approved Rubi unit catalog.
- Calendar and recruitment follow-up (2026-09-07): date pickers rendered inside dialogs now accept
  pointer interaction above the modal layer. Requesting-unit choices use HR units, evaluator fields
  use current employee names through a typeable combobox, and applicant source choices include
  Jobinja and Jobvision with direct source links and source URL. A validated PDF/DOC/DOCX resume is
  stored for the browser session, appears in the HR Documents preview dataset, and can be opened from
  either the applicant or document row. External job-board ingestion and durable Documents storage
  remain Phase B integrations because no approved public API or credential contract is available.
- Notification-center follow-up (2026-09-07): the existing shell bell is connected from within the
  HR-owned workspace without changing shared shell ownership. Employee, dossier, organization and
  contextual dataset create/edit/delete operations plus profile-photo and applicant-resume updates
  publish populated session notifications with unread count, timestamp and a link back to the source
  section. Users can mark all as read or clear the session feed. Durable delivery and cross-device
  notifications remain gated on the public Notifications contract and HR persistence.
- Lifecycle, contract and dossier follow-up (2026-09-07): new hires are captured without selecting
  an existing employee and are promoted into the session employee list on save; the employee list
  is validated and restored from session storage across navigation and refresh. Company/branch
  replaces the onboarding template and the progress field is removed. Promotion reads current
  position and grade from the selected employee and updates both the employee list and assignment
  history. The lifecycle skill-map tab is removed. Separation uses the shared calendar plus yes/no
  asset and access controls. Final settlements enter a dedicated Finance approval queue and Finance
  approval synchronizes the lifecycle status. Contract types now cover fixed-term, contractor,
  consulting, internship, NDA, non-compete and intellectual-property agreements with relevant
  terms. Active contract rows export a real A4 PDF using the supplied Niyayesh Seir or Jahan Bastan
  logo, and expiry alerts derive the contract number from the selected employee. Personnel document
  forms use detailed document metadata, actual browser-session upload, automatic registration date
  and a public access option; dossier uploads are copied into the HR Documents view and sent through
  the existing public Documents API to the real `HUMAN_RESOURCES` archive when the authenticated
  user has the required document permission and reference options. The request
  center is reduced to one merged inbox/request tab with the combined fields. These linked changes
  remain session-only until the approved HR, Finance and Documents contracts are available.

## Phase B handoff

1. Obtain formal migration and central-doc handoff from PC-A/SALES-CONTRACTS-001; reserve
   exact schema, permission seed and shared contract files before editing.
2. Resolve DEC-OPEN-006/013: retention/residency/encryption, shifts, leave entitlement,
   manager delegation, sensitive classifications and minimal approved Finance input.
3. Publish versioned IAM HR grants and trusted employee/branch/manager scope resolution.
   Self/manager IDs must be resolved on the server, never accepted from request bodies.
4. Implement real FK-backed repositories, encryption and unique personnel/user constraints;
   atomically persist version, idempotency fingerprint/receipt, history, audit and outbox.
   No hard delete; protect closed contracts and approved attendance/leave/review history.
5. Bind the consumer ports to public Documents/Legal Entity/Master Data/IAM/Finance contracts.
   Store stable document references only. No binary, storage key or private URL in HR.
   Require CLEAN at access time, audited sensitive reads and permission-filtered real exports.
6. Replace preview UI with authenticated scoped DTOs, server pagination and mutations;
   test unauthorized/forbidden/conflict, replay/body mismatch, concurrency and database rollback.
7. Implement approved workflow details beyond this foundation: candidate conversion uniqueness,
   onboarding access provisioning via IAM, device/mobile check-in ingestion, attendance
   calculations, shift recurrence, entitlements, statutory payroll/localization and reporting.
   Do not interpret generic preview forms as finished operational workflows.
8. Coordinate central navigation mapping for `/hr`, final accessibility/performance acceptance
   and removal of the old foundation landing after its shared route tests can be updated.

## Changed files

- `apps/api/src/hr/hr.entities.ts`
- `apps/api/src/hr/hr.domain.ts`
- `apps/api/src/hr/hr.policy.ts`
- `apps/api/src/hr/hr.ports.ts`
- `apps/api/src/hr/hr.application.ts`
- `apps/api/src/hr/hr.domain.spec.ts`
- `apps/api/src/hr/hr.application.spec.ts`
- `apps/api/src/hr/hr.boundary.spec.ts`
- `apps/web/src/modules/hr/hr.model.ts`
- `apps/web/src/modules/hr/frappe-workspaces.ts`
- `apps/web/src/modules/hr/frappe-workspace.tsx`
- `apps/web/src/modules/hr/frappe-workspace.module.css`
- `apps/web/src/modules/hr/frappe-workspaces.spec.tsx`
- `apps/web/src/modules/hr/hr-workspace.tsx`
- `apps/web/src/modules/hr/hr-workspace.module.css`
- `apps/web/src/modules/hr/hr-workspace.spec.tsx`
- `apps/web/src/modules/hr/new-employee-dialog.tsx`
- `apps/web/src/modules/hr/organization-chart.tsx`
- `apps/web/src/modules/hr/organization-catalog.tsx`
- `apps/web/src/modules/hr/contextual-hr-form.tsx`
- `apps/web/src/modules/hr/hr-xlsx.ts`
- `apps/web/src/modules/hr/hr-xlsx.spec.ts`
- `apps/web/src/modules/hr/hr-notifications.tsx`
- `apps/web/src/modules/hr/hr-notifications.spec.ts`
- `apps/web/src/modules/hr/hr-preview-data.ts`
- `apps/web/src/modules/hr/hr-preview-data.spec.ts`
- `apps/web/src/modules/hr/contract-logos.ts`
- `apps/web/src/modules/hr/hr-contract-pdf.ts`
- `apps/web/src/modules/hr/hr-contract-pdf.spec.ts`
- `apps/web/src/modules/hr/hr-documents-integration.ts`
- `apps/web/src/modules/hr/hr-documents-integration.spec.ts`
- `apps/web/src/app/(crm)/hr/page.tsx`
- `apps/web/src/app/(crm)/hr/loading.tsx`
- `apps/web/src/app/(crm)/hr/error.tsx`
- `apps/web/src/app/(crm)/human-resources/page.tsx`
- `apps/web/src/lib/environment.ts`
- `apps/web/src/lib/environment.spec.ts`
- `apps/web/src/lib/navigation.ts`
- `apps/web/src/lib/navigation.spec.ts`
- `apps/web/src/components/layout/app-shell.tsx`
- `apps/web/src/components/ui/date-picker.tsx`
- `docs/tasks/HR-001.md`

## HR-002 — time, equipment and employee development follow-up

- PC-B / codex/pc-b-hr-time-development, based on HR a44d622. Tax/benefits hidden from hub and routes; fleet under assets. Removed manual import tab, shift requests, development feedback and expense approval tab. Mission moved to expenses.
- Equipment condition options corrected. Holiday calendar picker consumes holiday names. Leave duration derived inclusively from dates; correction reads attendance; device sync timestamp cannot be entered manually and remains unsynchronized until an actual device adapter exists.
- Shift definitions include employee/date range; read-only Persian month calendar derives assignments from these definitions. Calendar export uses shift records.
- Training event/result fields merged with program form, opened by the program details action. Weighted checklist goals support multiple items and computed achieved weight / total weight; manager-only authorization is pending trusted IAM HR grants.
- Generic XLSX export and bounded XLSX import added to operational lists; history/calendar is export-only. No finance/payroll import. Parser supports stored/deflated worksheets and shared strings, rejects formulas/external links/macros and oversized input. Inputs currently persist in browser session, not HR database.
- Time/expense form submissions upsert the unified request inbox. Earlier onboarding/promotion/documents links preserved. Historical rows of earlier shift/training schemas retained and expanded without deleting user values.
- Remaining: HR durable API/repositories, IAM manager enforcement, Finance posting, Master Data/Legal Entity adapters, device and external job-board integrations need the published public contracts and handoff. Excel import currently adds local records and does not execute every lifecycle side-effect. These are not claimed complete.
`nValidation: 63 passing HR tests, Web lint/typecheck and production build passed. No migration. External contract and role enforcement work remains open.

HR-002 follow-up: removed standalone personnel documents hub entry and normalized its old section route to home. Employee dossier documents and archive integration retained.

HR-003: Every active HR section now exposes overview, operations and reports. Counts derive from current section rows. Report selection/search applies to preview and downloaded multipage Persian raster PDF. Employee and live organization records are supplied directly; files remain represented by labels. PDF byte structure tested; browser visual PDF QA not performed. Existing session persistence limitations unchanged.

HR-004: Employee names link directly to their profile. Attendance/leave/shift/contract/mission/overtime/performance/equipment/requests/payslips are filtered by employee reference; explicit personnel ID takes precedence, legacy name match allowed only when unique. Financial summary uses the employee payroll slip. Manual dossier rows use employee-specific storage keys. Anonymous old shared rows retained in storage but not attributed. Unknown employee route does not fall back to another profile. PDF reports use the same scoped records. 69 tests pass; no migration. This is UI/session scoping, not replacement for server IAM authorization.

HR-004 follow-up: removed the three-button overview/operations/reports switcher from the top of HR sections. Operations render directly; statistics and PDF export remain available in one collapsed panel after the section content.
