# HR-010 — Match HR record actions to Master Data

PC-B / `codex/pc-b-hr-operation-buttons`, based on current develop/runtime `0261b91` in `C:/Users/admin/Rubi-hr115-integration`.

HR tables previously hid Edit/Delete behind an ellipsis menu. They now show direct outlined small buttons using the same shared UI Button and Eye/FilePenLine/Trash2 icons as Master Data. Actions align to the left in RTL with 8px spacing; intrinsic width keeps them together within the horizontally scrollable table. Delete uses the shared destructive text color, including hover and dark theme tokens.

The existing record callbacks, optional View visibility, permission/capability checks, row selection/export and soft-delete confirmation remain in place. Busy tables disable their record action buttons. Employee lists, organization catalogs, operational lists and employee dossier tables all consume the same HR table component. No Master Data, app shell, API, IAM, contract, database, migration or dependency code changes.

Validation: all 97 existing HR/shared-Button tests passed, plus full Web lint and typecheck. The in-app browser failed to attach twice; authenticated visual/interaction QA is not claimed. Production build and runtime fingerprint checks are recorded in the PR/local handoff after activation. No test or business records, user grants or passwords were changed.

PC-B Uniting handed over Web3100 for this additive change. To rebuild only this checkout's Web while retaining API4190 and the existing database/documents:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File infrastructure/scripts/start-hr-local.ps1 -Build -SkipApi -Port 3100 -ApiPort 4190
```

Confirm `/api/hr-runtime` matches this branch's HEAD and API4190 remains healthy. Preserve `rubi_hr_current_20260908` and `C:/Users/admin/AppData/Local/Rubi/hr007-documents`. Push for review; no merge is requested in this task.
