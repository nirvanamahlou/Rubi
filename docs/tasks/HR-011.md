# HR-011 — Connect expense mission references

PC-B / `codex/pc-b-hr-expense-mission-link`, stacked on HR-010 `40c362e` in `C:/Users/admin/Rubi-hr115-integration`.

The independent-expense form now offers actual HR missions using their record IDs, with code, employee, destination and trip dates in each option. Both current `expenses.mission` and legacy `time.mission` records are supported. Options are scoped to the selected IAM branch, company and employee. Legacy missions without an explicit company inherit their employee's company; explicit historical company references take precedence. Changing the employee or company clears a stale selection. An empty list explains which selection to check, and an expense may still be recorded without a mission.

Expense references can be attached, replaced or cleared while editing a permitted draft. The existing parent FK is persisted for travel, advances and claims; the API validates the parent type, employee and company and derives the displayed advance reference from the actual parent code. Related expense records remain queryable from their mission, and the expense detail links back to the mission dossier. Approval, final-state restrictions, optimistic versions, permissions, audit and soft-delete behavior remain enforced, including rejecting a combined approval/cancellation and parent change.

Scope is limited to HR Web/API and regression tests. No shared contracts, schema, migrations, dependencies, IAM grants or other module changes. Read-only inspection used the current local HR database; mutation tests use a separately created, randomly named PostgreSQL test database that is dropped after the suite. No business records, passwords or document storage were changed for QA.

Validation: 99 HR Web tests and 80 HR API tests passed, including all 21 opt-in PostgreSQL integration tests. The new integration case covers create, reload, query-by-parent, attach/replace/clear, legacy missions, wrong company/employee/type, stale version, missing permission and final/approval guards. An initial concurrent run timed out in Docker setup and an existing Web test; isolated reruns passed. Web/API lint, typecheck, production build and final runtime/browser evidence accompany the review handoff.

The current runtime is retained on Web3100/API4190, database `rubi_hr_current_20260908`, documents `C:/Users/admin/AppData/Local/Rubi/hr007-documents`. After the API build, activate only this owned checkout:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File infrastructure/scripts/start-hr-local.ps1 -Build -Port 3100 -ApiPort 4190 -ApiEnvFile C:/Users/admin/Rubi-hr-foundation/apps/api/.env -DatabaseName rubi_hr_current_20260908 -DocumentStorageRoot C:/Users/admin/AppData/Local/Rubi/hr007-documents
```

Verify `/api/hr-runtime` matches HEAD and API health succeeds. PC-B Uniting has been notified of the continuing runtime reservation. Push for stacked review against HR-010; no merge is requested.
