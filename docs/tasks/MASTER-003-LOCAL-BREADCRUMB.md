# حذف مسیر آبی تکراری اطلاعات پایه — PC-B

وضعیت: آماده Review؛ انتشار با تأیید کاربر در 2026-08-31 روی `codex/pc-b-master-data-breadcrumb-cleanup`، با Base شاخه شریک‌ها/PR #45. والدها تغییر نمی‌کنند و قبل از آن‌ها Merge نمی‌شود.

## محدوده

- مسیر آبی `eyebrow` از سربرگ هشت بخش اطلاعات پایه و نمایش عمومی زیر‌بخش‌ها حذف شد.
- مسیر اصلی AppShell، عنوان، توضیحات، تب‌ها و دکمه «همه بخش‌ها» حفظ شدند.
- ورودی استفاده‌نشده عنوان بخش جغرافیا و ارسال آن از Workspace حذف شدند.
- فایل‌های مشترک AppShell و PageHeader، Customers، API، دیتابیس، تنظیمات و قفل‌های
  مرکزی برای این درخواست تغییر نکردند. تغییرات محلی قبلی حفظ شدند.
- به‌دلیل اشتراک Workspace با چت اصلی، شاخه، سرورها و اسناد تخصیص مرکزی تغییر نکردند.

## بررسی

- ۱۴۶ تست اطلاعات پایه در ۲۴ فایل: موفق؛ شامل ۱۰ بررسی جدید سربرگ و پیمایش.
- Typecheck وب و lint فایل‌های این اصلاح: موفق.
- `git diff --check` و بررسی قالب تست جدید: موفق.
- ساخت Production وب با webpack در کپی موقت جداگانه: موفق، شامل ۳۴ صفحه.
  پوشه `.next` سرور اصلی دست‌نخورده ماند.
- HTML تولیدشده هر هشت بخش بررسی شد: دقیقاً یک مسیر اصلی و بدون عنوان آبی تکراری.
- تست تعاملی مرورگر در این درخواست اجرا نشد.
