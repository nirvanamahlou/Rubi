# MASTER-003G — Master Data UX Consolidation

## مالکیت و پشته

- مالک: `PC-B`
- Branch: `codex/pc-b-master-data-ux-consolidation`
- والد مستقیم: `MASTER-003F-ACCOMMODATION` / Draft PR #32
- محدوده: فقط `apps/web/**`، تست‌های Web و اسناد همین Slice
- بدون تغییر Database، Migration، Seed، API Contract، Dependency یا Lockfile

## خروجی رابط

- برچسب داخلی `MASTER-003 · PC-B` از Header صفحه اصلی اطلاعات پایه حذف شد.
- تب مستقل «نرخ و تاریخچه ارز» حذف و تاریخچه داخل پنجره جزئیات ارز قرار گرفت.
- کلیک روی نام یا عملیات مشاهده هر ارز، تاریخچه واقعی Backend را برای هر دو حالت
  مبدأ/مقصد می‌خواند؛ نمودار براساس جفت ارز و نوع نرخ انتخابی و بازه ۳۰، ۹۰ یا ۳۶۵
  روزه نمایش داده می‌شود.
- نرخ جاری، تغییر نسبت به نرخ تأییدشده قبل، زمان مشاهده، جدول تاریخچه و Audit نرخ در
  همان پنجره نمایش داده می‌شوند. مقدار `isAuthoritative=false` حفظ شده است.
- «شهرها» و «استان‌ها و نواحی» در یک تب بالادستی «شهرها و استان‌ها» ادغام شدند؛
  نوع رکورد داخل همان بخش انتخاب می‌شود و مدل/FKهای مستقل بدون تغییر باقی مانده‌اند.
- تب‌های مستقل پروفایل تأمین‌کننده، کارگزار و هتل حذف شدند. کلیک روی نام رکورد یا
  عملیات مشاهده، پروفایل کامل را در Dialog مشترک RTL و Responsive باز می‌کند.
- تب مستقل «اطلاعات تماس» از رابط سازمان‌ها و تأمین‌کنندگان حذف شد. هیچ داده یا
  جدول مخاطب حذف نشده و این تغییر Migration مخرب ندارد.
- شمارنده‌ها و برچسب‌های کارت‌های Hub با زیرمجموعه‌های قابل مشاهده هماهنگ شدند.

## کنترل کیفیت

- Web tests: `114/114`
- Full monorepo tests: `366/366`
- Monorepo Typecheck: موفق
- Production Build: موفق؛ هر هشت Route اطلاعات پایه SSG شدند.
- ESLint تمام فایل‌های تغییرکرده: موفق
- Full Web Lint فقط روی ایراد قدیمی و خارج از Slice در
  `apps/web/src/components/ui/date-picker.tsx` متوقف می‌شود.
- مسیرهای بدون Session به Login هدایت می‌شوند و API Health پاسخ `200` می‌دهد.
- Scope scan: بدون تغییر Customers، Prisma/Migration، Package/Lockfile یا داده حساس

## قاعده ادغام

این Slice به‌صورت Stacked روی PR #32 می‌ماند و پیش از والدهای #32، #31، #30، #29،
#28 و #25 Merge نمی‌شود. پس از Merge هر والد، Base طبق ترتیب پشته به‌روزرسانی می‌شود.
