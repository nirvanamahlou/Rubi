# SEARCH-SHORTCUT-CONTRAST-0909
PC-A. Only AppShell global search shortcut styling changes.

The kbd inherited the company header's light text on a surface background. It now explicitly uses paired surface/foreground and input-border tokens, slightly larger semibold type, LTR Ctrl + K, and a nonshrinking box. Visible from the small breakpoint rather than only large screens. Keyboard event handler and search behavior unchanged. No global CSS or palette edit.

25 existing layout tests, scoped ESLint, typecheck and production build passed; Web3100 refreshed. Preserve PDF environment. No backend, data, permissions or public publication. Local commit only while public-origin authorization is pending.
