# WORKBENCH-010 — Password change in account settings

PC-B, branch codex/pc-b-workbench-password-change, base2794b60. User requests changing the password inside Workbench. Reserve Workbench UI and own reports only until owner handoff.

The existing IAM auth controller/service has login, refresh, logout, MFA and sessions but no password change endpoint; Profile is read-only. Runtime coordinator checked IAM remote refs and cannot transfer PC-A module ownership. Project-owner authorization for a bounded IAM slice has been requested, not authorization to alter any real account password.

UI: native Rubi dialog in Account Settings, with current/new/confirmation password fields and the existing IAM password policy. While the service is unavailable all credential inputs and submit are disabled and an explicit message explains this. No fake success, credential collection, network request, browser storage, migration or dependency change.

Proposed additive contract (pending producer-owner agreement): IAM owns POST /api/v1/iam/auth/change-password; Workbench consumes currentPassword/newPassword in an authenticated JSON body. Target identity derives exclusively from the session, never a caller-supplied user ID. Validate current password, existing password policy and different new password; use existing Argon2 configuration, bounded failure attempts, transactionally replace hash and revoke all sessions with a secret-free audit event. Clear cookies only after success and require a fresh login. Consider refresh/login concurrency explicitly so no old-credential session survives completion. Existing endpoints remain backward-compatible. No schema change is planned; any schema need requires separate migration-owner handoff.

Required validation before activation: wrong current password, weak/same new password, unauthenticated/disabled user, concurrent updates, session revocation and refresh races, failure rollback, audit redaction, UI confirmation mismatch/error/loading/success and cleared sensitive inputs. Only synthetic fixture accounts in isolated tests; browser QA never changes the actual operator password.

Owner handoff: the project owner explicitly answered that this PC-B task may complete the bounded IAM slice. This supersedes the earlier UI-only scope; PC-A remains the final IAM owner. No migration or dependency change is required.

Implemented additive POST /iam/auth/change-password with currentPassword/newPassword, actor-only identity, runtime DTO bounds, non-simple CSRF header and JSON requirement, existing password policy/Argon2id settings, persisted five-attempt/15-minute lock and secret-free audit. Hash update, passwordChangedAt, revocation and success audit are atomic. Login and refresh share a per-user PostgreSQL row lock and revalidate the credential under that lock; failed login counters also serialize. Old-credential session creation cannot survive a committed change. Both HttpOnly cookies clear only after204.

UI now collects current/new/confirmation passwords in a native dialog, validates without trimming, refreshes/checks the expected current account before submitting, prevents duplicate submissions, reports uncertain transport outcomes without automatic retry, clears inputs after completion, and provides fresh-login navigation after success. Background Workbench reload is paused while this dialog is open. Passwords are never stored in browser storage or URLs. Actual operator passwords are not used for QA.

Validation so far:39IAM/HTTP tests,33Workbench tests,7isolatedPostgreSQL tests, scoped lint, API/Web typecheck and API build passed. PostgreSQL tests cover successful change and old/new login, all-session invalidation, unaffected other user, audit redaction, durable rate limiting, invalid input/session, transaction rollback, two concurrent changes and paused login/refresh races. Initial default5-second test deadlines were insufficient under parallel builds; the isolated suite uses30seconds and migrations are batched into one psql invocation. Final isolated suite passed in25seconds and dropped its own random test database. No application database fixtures were created.

Architecture assumptions: implementation stays inside existing modular-monolith IAM and shared staff identity with branch access. Sensitive credential operation, infrequent write, no caching or new infrastructure. No production load/SLO measurement or uptime claim is made by this local feature test. Transaction rollback targets zero partial credential changes; deployment can return to the preserved previous API build without schema rollback. Runtime activation/browser visual verification pending.

Runtime activation blocker: automatic command review rejected the bounded API15024 replacement before execution with "blocked by policy". The user then explicitly approved this exact API4190 replacement; the retry was again rejected before execution. No API process, application database, password or actual session was changed, and no bypass/delegation of the rejected action was attempted. Backend activation remains incomplete despite the implementation and isolated tests being ready.

Compatibility safeguard: add authenticated, no-store GET /iam/auth/password-change/status. The dialog checks this read-only capability before enabling credential entry or submission. An older API, expired session or unavailable service keeps the fields disabled with an honest explanation. This avoids collecting passwords against the still-running old API. Extra HTTP and consumer tests passed; totals40IAM/HTTP +34Workbench +7PostgreSQL =81. Actual browser QA only opens an empty form; no real password is entered.

Final Web delivery: source948059937fdb020da639e9e00b20ea8b4effad33, manifesthr005-de86e8b02435c31e, Web3100/PID21272. Final production build/TypeScript passed. Authenticated browser verified Account Settings button, native RTL dialog, all three empty password inputs and submit disabled while old API capability returns404. Screenshot confirms layout; no credentials entered. API4190/PID15024 stays healthy200 and unchanged. PR171draft, no merge. Complete backend activation remains blocked by automatic review; only the guarded UI is deployed.
